{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the tool"
    },
    "file": {
      "type": "string",
      "description": "Python filename where the tool is stored. Should be a relative path. For example: tools/search.py, generated_tools/summarizer.py"
    },
    "is_ai_generated": {
      "type": "boolean",
      "description": "Set to true if this tools was generated by AI"
    },
    "parameters": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string",
          "description": "..."
        }
      },
      "description": "The parameters the python function (tool) accepts"
    }
  },
  "required": ["name", "age"]
}